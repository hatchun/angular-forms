import { GroupErrorDirective } from './group-error.directive';

describe('GroupErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new GroupErrorDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
