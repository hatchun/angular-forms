import { ControlErrorDirective } from './control-error.directive';

describe('ControlErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlErrorDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
